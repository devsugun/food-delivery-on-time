import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Food delivery on-time',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
